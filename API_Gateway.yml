spring:
  application:
    name: API_Gateway
  cloud:
    gateway:
      routes:
        - id: api-1
          uri: lb://WELCOME-SERVICE
          predicates:
            - Path=/welcome
        - id: api-2
          uri: lb://Greet-Services
          predicates:
            - Path=/greet

server:
  port: 3333


